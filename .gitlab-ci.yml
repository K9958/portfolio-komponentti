stages:
  - build
  - deploy

build:
  image: node:14
  stage: build
  script:
    - cd FinalAssignment/shop_app
    - npm install
    - npm run build CI=""
    - ls -l
    - ls -l build/
    - cp -r ./build /build
    - ls -l /build
  only:
    - main
  artifacts:
    paths:
      - build/
  tags:
    - docker

deploy:
  stage: deploy
  script:
    - cd build/
    - ls -l
    - cp -r ./* $APP_FOLDER